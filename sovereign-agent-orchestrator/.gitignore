# Local Python environments and caches
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local configuration and runtime state
.env
orchestrator.db
*.db
# Runtime job workspace only — anchored to repo root so it does not match app/workspace/
/workspace/
# Machine-specific llama-swap config (absolute model paths); the .example is tracked
config/llama-swap.yaml
*.log
ollama-data/

# IDE and operating-system files
.vscode/
.idea/
.DS_Store
Thumbs.db